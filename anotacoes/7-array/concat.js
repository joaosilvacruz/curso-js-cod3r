// O método concat, concatena arrays.
// Pode ser adicionado mais parâmetros para a concatenação

const amigos = ['joao', 'paulo', 'pedro', 'caio']
const amigas = ['thata', 'stefani', 'mirela', 'maria']
const todos = amigos.concat(amigas, 'fulano')
console.log(todos)